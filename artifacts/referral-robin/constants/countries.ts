// Shared between the Explore country filter chips and the admin "add a
// company" form's country picker. Most existing brands are UK, but this is
// explicit going forward rather than assumed.
export const BRAND_COUNTRIES = ['UK', 'US', 'EU', 'Other'] as const;
